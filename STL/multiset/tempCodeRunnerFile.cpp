 // string s = "klno";
    // char a = s[0];
    // int mask = 0;
    // for(char c : s) mask |= (1 << (c-a));
    // for(int i=0;i<5;i++)
    //     if((mask & (1<<i)) == 0) cout << "Missing letter: " << char(a+i) << endl;
